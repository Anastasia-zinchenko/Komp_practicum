import numpy as np

# Генерация случайных параметров параболического цилиндра, плоскости, прямой и координат точки
# Пример: параболический цилиндр в виде y = x^2, плоскость вида z = 2x + 1, прямая вида x = y = z, точка (1, 2, 3)

# Параметры параболического цилиндра: y = ax^2
a = np.random.randint(1, 10)  # случайный коэффициент "a"

# Параметры плоскости: z = mx + n
m = np.random.randint(1, 5)  # случайный коэффициент "m"
n = np.random.randint(1, 5)  # случайный коэффициент "n"

# Параметры прямой: x = y = z

# Координаты случайной точки
point = np.random.randint(-5, 5, 3)

# Функция для проверки пересечения параболического цилиндра с плоскостью
def check_intersection_cylinder_plane(a, m, n):
    # Пересечение параболического цилиндра с плоскостью происходит, если m*a*x^2 - z + n = 0 имеет решение
    discriminant = m * a * point[0] ** 2 - point[2] + n
    if discriminant == 0:
        return True
    else:
        return False

# Функция для проверки принадлежности точки параболическому цилиндру
def check_point_belongs_cylinder(a, point):
    # Проверка принадлежности точки параболическому цилиндру
    if point[1] == a * point[0] ** 2:
        return True
    else:
        return False

# Проверка и вывод результатов
if check_intersection_cylinder_plane(a, m, n):
    print("Плоскость пересекает заданный параболический цилиндр.")
else:
    print("Плоскость НЕ пересекает заданный параболический цилиндр.")

if check_point_belongs_cylinder(a, point):
    print("Точка принадлежит параболическому цилиндру.")
else:
    print("Точка НЕ принадлежит параболическому цилиндру."
